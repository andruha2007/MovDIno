movdino/
-core/			# Ядро проекта, из которого происходит запуск
--interpreter.h/.c	# Интерпретатор, обработка состояний
--executor.h/.c		# Обработчик всех команд проекта

-data/			# Данные проекта
--structures.h		# Структуры проекта
--field.h/.c		# Объявление поля, печать освобождение, проверка символов
--history.h/.c		# История полей для отката

-parser/		# Парсер команд из файла и обработчик
--parser.h/.c		# Проверка на левые пробелы, комментарии
--specific_parser.h/.c	# Обработка исключений, запись команды
--writer.h/.c		# Функции записи

-utils/			# Различные проверки
--colors.h		# Библиотека цветов
--common.h/.c		# Вообще бесполезный файл, можно куда-то перенести  
--errors.h/.c		# Функции ошибок
--validator.h/.c	# Функции проверок для парсера

-movdino.c		# База, чтение файла и тд

// Дополнительная секция
- ачивки (умный в гору не пойдет; не опущен, а занижен)
- цветное поле